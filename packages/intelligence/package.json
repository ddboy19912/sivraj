{
  "name": "@sivraj/intelligence",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "check": "pnpm typecheck",
    "lint": "echo \"intelligence lint not configured yet\"",
    "test": "vitest run",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@sivraj/core": "workspace:*",
    "@sivraj/llm": "workspace:*"
  },
  "devDependencies": {
    "@types/node": "^24.12.4",
    "typescript": "~6.0.3",
    "vitest": "^4.1.6"
  }
}
