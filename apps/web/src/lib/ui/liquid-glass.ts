/** Frosted panel glass — light fill, strong backdrop blur (onboarding panel recipe). */
export const liquidGlass = "liquid-glass";

/** Same blur with a lighter fill for inputs, buttons, and chips. */
export const liquidGlassDense = "liquid-glass-dense";
