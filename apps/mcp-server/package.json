{
  "name": "@sivraj/mcp-server",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "check": "pnpm typecheck",
    "dev": "tsx src/index.ts",
    "lint": "echo \"mcp-server lint not configured yet\"",
    "test": "vitest run",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.0.0",
    "@sivraj/config": "workspace:*",
    "@sivraj/crypto-seal": "workspace:*",
    "@mysten/sui": "^2.16.3",
    "dotenv": "^17.4.2",
    "zod": "^3.24.0"
  },
  "devDependencies": {
    "@types/node": "^24.12.4",
    "tsx": "^4.22.0",
    "typescript": "~6.0.3",
    "vitest": "^4.1.6"
  }
}
