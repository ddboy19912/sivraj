# Sivraj Docs Site

Docusaurus site for the root `docs/` directory.

## Commands

- `pnpm --filter @sivraj/docs dev` - start the local docs server.
- `pnpm --filter @sivraj/docs build` - build the static docs site.
- `pnpm --filter @sivraj/docs serve` - preview the production build.
